<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haebub.dao.admin.ChartTestMapper">
    <!--<select id="georgiCount" resultType="int">
        SELECT count(*) FROM employees
        WHERE first_name = 'Georgi'
    </select>
    <select id="getHireChart" resultType="hashmap">

        SELECT COUNT(if(YEAR(hire_date) = '1985',1,null))   as 'year1'
                ,COUNT(if(YEAR(hire_date) = '1986',1,null)) as 'year2'
                ,COUNT(if(YEAR(hire_date) = '1987',1,null)) as 'year3'
                ,COUNT(if(YEAR(hire_date) = '1988',1,null)) as 'year4'
                ,COUNT(if(YEAR(hire_date) = '1989',1,null)) as 'year5'
                ,COUNT(if(YEAR(hire_date) = '1990',1,null)) as 'year6'
                ,COUNT(if(YEAR(hire_date) = '1991',1,null)) as 'year7'
                ,COUNT(if(YEAR(hire_date) = '1992',1,null)) as 'year8'
        FROM employees
        GROUP BY gender;
    </select>-->
</mapper>