<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haebub.dao.one.OneMapper">
    <insert id="oneinsert">
        insert into one( otitle
        ,ocontent
        ,oregdate
        ,osecret
        ,uid
        ,lid
        )
        values(
        #{otitle}
        ,#{ocontent}
        ,sysdate()
        ,#{osecret}
        ,(select uid from user where uid=#{uid})
        ,(select lid from lecture where lid=#{lid})
        )

    </insert>

</mapper>